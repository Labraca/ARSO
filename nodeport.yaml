apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  annotations:
    rancher.io/globalDNS.warso.es
  labels:
    app: nginx
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
