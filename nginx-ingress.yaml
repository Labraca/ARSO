apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nginx-ingress
  annotations:
    rancher.io/globalDNS.hostname: warso.es
spec:
  rules:
    - host: nginx.apps.warso.es
      http:
        paths:
          - backend:
              serviceName: nginx-nodeport
              servicePort: 80
