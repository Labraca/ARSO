apiVersion: v1
kind: Service
metadata:
  name: wordpress-nodeport
  labels:
    app: wordpress
spec:
  type: NodePort
  selector:
    app: wordpress
  ports:
    - nodePort: 30004
      port: 80
      targetPort: 80
      name: wordpress-port
